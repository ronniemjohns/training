var expect = require('chai').expect;

describe('GAME INSTANCE FUNCTIONS', function() {
    describe('checkGameStatus', function() {
       it('should tell me when game is over'); // note no FUNCTION yet, which marks it as pending.

       xit('alternatively add x before it or describe to mark as pending');
    });
});